<UserControl x:Class="EditorSupport.CodeCompletion.CompletionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EditorSupport.CodeCompletion"
             mc:Ignorable="d"
             x:Name="completionList">
    <Grid>
        <ListBox x:Name="lbCompletions" ItemsSource="{Binding ElementName=completionList, Path=Completions}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Image}" Width="16" Height="16" Margin="0 0 2 0" />
                        <ContentControl Content="{Binding Content}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
